nacos:
  server-addr: http://121.37.25.151:8848
  namespace: 3c846ab1-b76b-423b-91c6-6dc5d5954b11
spring:
  application:
    name: ums
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        file-extension: yaml
        namespace: ${nacos.namespace}
      discovery:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
  security:
    oauth2:
      client:
        registration:
          login-client:
            client-id: admin
            client-secret: e10adc3949ba59abbe56e057f20f883e
            authorization-grant-type: password